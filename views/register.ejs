<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register page</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.6.0/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Custom CSS */
        body {
            background-color: #f8f9fa; /* Light gray background */
            font-family: Arial, sans-serif; /* Font style */
        }
        .d1 {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .card {
            border: none; /* Remove border */
            border-radius: 15px; /* Rounded corners */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Soft shadow effect */
            width: 500px; /* Adjust card width */
            height: auto; /* Adjust card height */
        }
        .card-title {
            color: #333; /* Dark text color */
        }
        .form-control {
            margin-left: 20px;
            border: none; /* Remove border */
            border-radius: 20px; /* Rounded input fields */
            padding: 12px 20px; /* Add padding */
            background-color: #f0f0f0; /* Light gray background */
            box-shadow: none; /* Remove shadow */
            width: 80%; /* Adjust input width */
        }
        .form-group {
            margin-bottom: 20px; /* Add margin bottom for spacing between input fields */
        }
        .btn {
            border-radius: 20px; /* Rounded buttons */
            padding: 10px 30px; /* Add padding */
            font-weight: bold; /* Bold font */
        }
        .btn-danger {
            background-color: #ff5555; /* Red background color */
            border: none; /* Remove border */
            margin-left: 20px;
        }
        .btn-danger:hover {
            background-color: #ff3333; /* Darker red on hover */
        }
        .btn-success {
            background-color: #28a745; /* Green background color */
            border: none; /* Remove border */
        }
        .btn-success:hover {
            background-color: #218838; /* Darker green on hover */
        }
    </style>
</head>
<body>
    <div class="d1">
        <div class="card">
            <div class="card-body">
                <h3 class="card-title text-center mb-4" style="margin-left: 20px;">Register Now</h3>
                <form action="/home" method="post">
                    <div class="form-group">
                        <input type="text" name="username" id="username" placeholder="Your Name" class="form-control">
                    </div>
                    <div class="form-group">
                        <input type="email" name="email" id="" placeholder="Your email" class="form-control">
                    </div>
                    <div class="form-group">
                        <input type="number" name="phone" id="" placeholder="Phone Number"  class="form-control">
                    </div>
                    <div class="form-group">
                        <input type="password" name="password" id="" placeholder="Password" class="form-control">
                    </div>
                    <div class="form-group">
                        <input type="password" name="cpassword" id="" placeholder="Password again" class="form-control">
                    </div>
                    <div class="form-group text-center">
                        <button type="reset" class="btn btn-danger mr-3">Clear</button>
                        <button type="submit" class="btn btn-success">Register</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <!-- Bootstrap JS (optional) -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.6.0/js/bootstrap.min.js"></script>
</body>
</html>
